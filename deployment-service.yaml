apiVersion: v1
data:
  .dockerconfigjson: eyJhdXRocyI6eyJkb2NrZXIuaW8iOnsidXNlcm5hbWUiOiJ2ZW5rYXRlc2hyZWRkeTY3OSIsInBhc3N3b3JkIjoiVmVua3kyQFNwaWR5IiwiZW1haWwiOiJ2ZW5rYXRlc2gucmVkZHk2NzlAZ21haWwuY29tIiwiYXV0aCI6ImRtVnVhMkYwWlhOb2NtVmtaSGsyTnprNlZtVnVhM2t5UUZOd2FXUjUifX19
kind: Secret
metadata:
  creationTimestamp: "2024-06-10T02:30:51Z"
  name: docker-cred
  namespace: default
  resourceVersion: "247711"
  uid: bbf8937c-7031-4996-82d4-32ca4b57c12c
type: kubernetes.io/dockerconfigjson

---

apiVersion: apps/v1
kind: Deployment # Kubernetes resource kind we are creating
metadata:
  name: boardgame-deployment
spec:
  selector:
    matchLabels:
      app: boardgame
  replicas: 3 # Number of replicas that will be created for this deployment
  template:
    metadata:
      labels:
        app: boardgame
    spec:
      imagePullSecrets:
        - name: docker-cred
      containers:
        - name: boardgame
          image:  venkateshreddy679/board-game:0# Image that will be used to containers in the cluster
          imagePullPolicy: Always
          ports:
            - containerPort: 8080 # The port that the container is running on in the cluster


---

apiVersion: v1 # Kubernetes API version
kind: Service # Kubernetes resource kind we are creating
metadata: # Metadata of the resource kind we are creating
  name: boardgame-ssvc
spec:
  selector:
    app: boardgame
  ports:
    - protocol: "TCP"
      port: 8080 # The port that the service is running on in the cluster
      targetPort: 8080 # The port exposed by the service
  type: LoadBalancer # type of the service.
